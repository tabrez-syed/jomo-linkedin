{
  "name": "jomo-linkedin",
  "version": "0.1.0",
  "description": "Platform-native friction for LinkedIn — pause-gate prompt at end of feed plus an in-page session timer",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "compile": "wxt prepare && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^24.3.0",
    "jsdom": "^29.1.0",
    "typescript": "^5.3.0",
    "vitest": "^3.2.4",
    "wxt": "latest"
  }
}
